# Optional module-level override only.
# Prefer managing keys in root ../.env instead.
OPENAI_API_KEY=sk-...
DEEPSEEK_API_KEY=sk-...
DEEPSEEK_BASE_URL=https://api.deepseek.com
MODEL=deepseek-v4-flash
MCP_URL=http://localhost:8000
MCP_SERVER_API_KEY=replace-with-same-secret-as-mcp-server

# Optional
# MD_DIR=pipeline/data/markdown
